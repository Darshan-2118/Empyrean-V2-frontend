# Backend URL for the Vite dev/preview proxy
# NOTE: VITE_-prefixed variables are embedded in the client bundle and ARE
# exposed to the browser. This value is only meant for local development,
# where the Vite proxy forwards /api, /health and /ws requests to the backend.
VITE_BACKEND_URL=http://localhost:port_number
